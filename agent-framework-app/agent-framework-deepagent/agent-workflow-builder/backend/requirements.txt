# Microsoft Agent Framework (will pull in its own dependencies including pydantic, opentelemetry, etc.)
agent-framework

# Agent Framework Extensions (installed automatically with agent-framework base package)
# These are explicitly listed for documentation purposes - they were auto-installed as dependencies
# agent-framework-devui>=1.0.0b251001          # Development UI for testing agents/workflows
# agent-framework-copilotstudio>=1.0.0b251001  # Copilot Studio integration for future use
# agent-framework-azure-ai>=1.0.0b251001       # Azure AI Foundry integration (optional)
# agent-framework-a2a>=1.0.0b251001            # A2A protocol support (optional)

# Backend Framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12

# Authentication & Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# Database
sqlmodel>=0.0.22
alembic>=1.13.0
asyncpg>=0.29.0
psycopg2-binary>=2.9.0

# Azure integrations
azure-identity>=1.19.0
azure-keyvault-secrets>=4.7.0
openai>=1.0.0

# MCP Protocol support (package 'mcp' provides module 'modelcontextprotocol')
mcp>=1.16.0

# Utilities
python-dotenv>=1.0.0
httpx>=0.27.0
websockets>=13.0

# Development and testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-cov>=4.1.0
black>=24.10.0
ruff>=0.7.0
mypy>=1.7.0

# Monitoring and observability
prometheus-client>=0.19.0
structlog>=23.2.0
rich>=13.7.0

# CLI tools
click>=8.1.0
typer>=0.9.0